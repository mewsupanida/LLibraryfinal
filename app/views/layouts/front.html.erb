<!DOCTYPE html>
<html lang="en">

  <head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>Public Library</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "bootstrap/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag 'heroic-features', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag "front-style" %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "jquery/jquery.min.js" %>
    <%= javascript_include_tag "jquery/typeahead" %>
    <%= javascript_include_tag "bootstrap/js/bootstrap.bundle.min.js" %>

  </head>

  <body>

      <!-- Navigation -->
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
          <a class="navbar-brand" href="/">Public Library</a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
              <li class="nav-item">
                <%= link_to "Admin", new_user_session_path, :method => :get, :class => "nav-link" if !user_signed_in? %>
              </li>
              <li class="nav-item">
                <%= link_to "Words", words_path, :method => :get, :class => "nav-link" if user_signed_in? %>
              </li>
              <li class="nav-item">
                <%= link_to "Sign out", destroy_user_session_path, :method => :delete, :class => "nav-link" if user_signed_in? %>
              </li>
            </ul>
          </div>
        </div>
      </nav>

      <% if notice %>
        <p id="notice" class="notice alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-warning"><%= alert %></p>
      <% end %>

      <%= yield %>

      <!-- Footer -->
      <footer class="py-5 bg-dark">
        <div class="container">
          <p class="m-0 text-center text-white">Copyright &copy; Your Website 2020</p>
        </div>
        <!-- /.container -->
      </footer>

  </body>

</html>
